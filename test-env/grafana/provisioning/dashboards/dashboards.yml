apiVersion: 1

providers:
  - name: 'PgCache Dashboards'
    orgId: 1
    folder: 'PgCache'
    folderUid: 'pgcache'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards
